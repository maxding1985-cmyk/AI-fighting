# Commit Note - Add Browser UI E2E

- **Timestamp:** 2026-06-11 13:53:27 CST
- **Branch:** main
- **Commit message:** Add browser UI E2E coverage
- **Commit hash:** Pending until commit is created
- **Scope:** Add Playwright browser UI coverage for the two-player duel flow and wire it into CI.

## Summary

- Added a Playwright-based browser UI E2E test that drives two independent browser sessions through room creation, joining, rule generation/confirmation, battle verification, and exit confirmation.
- Added shared test server helpers so HTTP/API E2E and browser UI E2E reuse the same local server startup and JSON request utilities.
- Added `npm run test:ui`, Playwright dev dependency metadata, and CI steps to install Chromium and run UI E2E after the normal test suite.
- Updated README and `GAME_MVP_ROADMAP.md` to document the UI E2E workflow and reflect MVP test coverage progress.

## Changed Files

| File | Change | Notes |
|---|---|---|
| `tests/browserUi.e2e.test.js` | Added | Runs the two-player browser UI flow and validates player A movement/fire plus exit confirmation. |
| `tests/helpers/testServer.js` | Added | Shares local server lifecycle and JSON helpers between E2E tests. |
| `tests/serverApi.e2e.test.js` | Changed | Reuses shared test helpers to remove duplicated setup code. |
| `.github/workflows/test.yml` | Changed | Installs dependencies and Playwright Chromium, then runs `npm test` and `npm run test:ui`. |
| `package.json` | Changed | Adds `test:ui` script and Playwright dev dependency. |
| `package-lock.json` | Added | Locks Playwright dependency resolution for CI. |
| `README.md` | Changed | Documents browser UI E2E setup, command, coverage, and CI behavior. |
| `GAME_MVP_ROADMAP.md` | Changed | Marks E2E coverage as Testing and moves room cleanup to the next priority. |
| `commit-notes/2026-06-11_13-53-27_add-browser-ui-e2e.md` | Added | Records this commit's scope and validation. |

## Behavior / User Impact

- CI now validates the real browser path in addition to API and unit coverage.
- Developers can explicitly run `npm run test:ui` to verify the full room/battle/exit UI flow.
- The previously reported player A behavior regression is now protected through the UI, API, and service layers.

## Validation

- Ran `npm test`: 23 passed, 0 failed, 1 skipped; the skipped test is the UI E2E when `RUN_UI_E2E` is not set.
- Ran `npm run test:ui`: 1 passed, 0 failed, 0 skipped using Playwright Chromium.

## Follow-ups

- Add room cleanup / expiry as the next MVP 1 stability item.
- Extend UI E2E for error paths, mobile layout, and additional strategy prompts.
